let x = Math.random(); // This gives a random number
let y = Math.random()*6; // This gives a random number between 0 and 5
let z = Math.floor(Math.random()*6); // This gives a random number (Rounded down) between 0 and 5
let a = Math.floor(Math.random()*6) + 1; // This gives a random number (Rounded down) between 1 and 6 as if we are rolling a dice

console.log(x);
console.log(y);
console.log(z);
console.log(a);