let x = 3.14;
let y;
let z;
let a;
let b;
let c;
let d; 
let e;
let f;
let maximum;
let minimum;

y = Math.round(x); // Rounds off
console.log(y);

z = Math.floor(x); // Rounds down
console.log(z);

a = Math.ceil(x); // Rounds up
console.log(a);

b = Math.pow(x,2); // Raises variable by some power
console.log(b);

c = Math.sqrt(x); // Square root
console.log(c);

d =Math.abs(x); // Absolute Value (Distance from 0)
console.log(d);

maximum = Math.max(x,y,z);
console.log(maximum);

minimum = Math.min(x,y,z);
console.log(minimum);